name: cesa_dues_core
description: "Shared models, constants, services, and utilities for CESA DUES."
version: 0.0.1
publish_to: 'none'

resolution: workspace

environment:
  sdk: ^3.11.5

dependencies:
  flutter:
    sdk: flutter
  cloud_firestore: ^5.6.0
  firebase_auth: ^5.5.0
  intl: ^0.20.2
  json_annotation: ^4.9.0
  equatable: ^2.0.7
  google_sign_in: ^6.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  json_serializable: ^6.9.3
  build_runner: ^2.4.15

flutter:
  uses-material-design: true
